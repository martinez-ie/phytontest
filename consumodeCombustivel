# Consumo de combustível

# Solicitar a distância percorrida e a quantidade de combustível gasto
distancia = float(input("Digite a distância percorrida pelo carro (em km): "))
combustivel = float(input("Digite a quantidade de combustível gasto (em litros): "))

# Calcular o consumo em km/l
if combustivel > 0:  # Verificar se a quantidade de combustível é maior que zero
    consumo = distancia / combustivel
    print(f"\nO consumo do carro é de {consumo:.2f} km/l.")
else:
    print("\nA quantidade de combustível não pode ser zero ou negativa.")
